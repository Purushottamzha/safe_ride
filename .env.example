# PostgreSQL
POSTGRES_USER=saferide
POSTGRES_PASSWORD=saferide_secret_2024
POSTGRES_DB=saferide

# Redis
REDIS_PASSWORD=saferide_redis_2024

# Mosquitto MQTT
MOSQUITTO_PASSWORD=saferide-mqtt-backend-2024

# Grafana
GF_SECURITY_ADMIN_PASSWORD=admin

# OSRM (optional — routing engine)
# OSRM_DOWNLOAD_URL=https://... not set by default
