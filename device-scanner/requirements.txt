paho-mqtt>=1.6.1,<2

# NOTE: opencv-python-headless and pyzbar are intentionally omitted.
# QR decoding shells out to the `zbarimg` CLI (from the Termux `zbar`
# package) instead of using Python image libraries — see camera.py.
